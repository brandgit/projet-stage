import { StyleSheet, Text, View } from "react-native";
import React from "react";

export const BackgroundImageForm = ({ children }) => {
  return (
    <View>
      <Text>BackgroundImageForm</Text>
    </View>
  );
};

const styles = StyleSheet.create({});
